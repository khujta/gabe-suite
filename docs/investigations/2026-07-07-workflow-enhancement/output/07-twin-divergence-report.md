# Deliverable 7 — Twin Divergence Report (Gustify / Gastify)

- **Produced:** 2026-07-08, Fable 5 analysis run.
- **Rule of engagement (locked decision §2A.4):** every divergence is *flagged with a recommendation*; the **operator decides** each one. Nothing here is a unilateral convergence.
- Sources: repo scans of both projects (2026-07-08), transcript evidence, `.kdbp/` inventories.

| # | Divergence | Gustify | Gastify | Recommendation | Why |
|---|-----------|---------|---------|----------------|-----|
| T1 | **Monorepo layout** | true `apps/{api,web,mobile}` monorepo; alembic at root | flat: `backend/ frontend/ web/ mobile/ design-lab/` at root | **KEEP** (flag only) | Restructuring a live repo is high-cost/low-payoff; the manifest (`app_root`, paths) absorbs the difference. Evidence the difference already bites: gastify's `.gitignore` still carries dead gustify-shaped `apps/web/*` entries — clean those lines regardless |
| T2 | **Mockup surface location** | Storybook **in-tree** (`apps/web`, port 6006) — spike and production share one package | separate **design-lab** sandbox (port 6008) → port into `web/` | **KEEP both, name it in the manifest** (`lift_kind: in-tree` vs `cross-package`) | Both are working conventions with real investment (107 vs 99 stories). Unifying would mean rebuilding one of them; the SOP (Deliverable 3) parameterizes the lift instead. Note: gastify pays a real extra cost per lift (the port step) — if the operator ever wants convergence, gustify's in-tree shape is the cheaper end-state, but that is a product decision, not a hygiene fix |
| T3 | **`.kdbp/RULES.md`** | exists — 11 evidence-cited scar-tissue rules with `Detection:` commands | **absent** | **UNIFY — create gastify's RULES.md** (skeptic-verified precondition for the taste-graduation path) | gastify's P5/P6/P11 incidents (check-legacy, match-gustify icon scale, icon-size-everywhere) are exactly RULES.md material; today they have no home and recur. Smallest-first candidate |
| T4 | **Reuse/design manifest docs** | `docs/rebuild/ux/{DESIGN.md, STORYBOOK-STRUCTURE.md, REACT-STORYBOOK-WORKFLOW.md}`, `WEB-SCREEN-MAP.md`, `WEB-LAYOUT-POLICY.md`, nested `apps/web/CLAUDE.md` | none of these; nearest equivalents: `design-lab/README.md`, `docs/mockups/WEB-MIGRATION.md` | **UNIFY at the manifest level** — give gastify the three load-bearing ones: screen map, design ref, layout policy (as manifest fields, whatever the filenames) | Arc-3 recurrence (Deliverable 3 §1) traces directly to the missing screen map (wrong-reference port) and missing nested CLAUDE.md reuse rule |
| T5 | **Evidence convention** | run artifacts **gitignored** (`tests/web-e2e/artifacts/`), feature-named, lost once to a background cleanup | **committed** `tests/web-e2e/proof/` (248 screenshots, force-added), feature-named + 1 dated exception | **UNIFY on the reduced middle** (Deliverable 4 §3): manifests always committed; curated screenshot subset committed via un-ignore carve-out; full runs gitignored | Each twin holds half the right answer; the skeptic-refined convention takes gastify's durability and gustify's repo hygiene |
| T6 | **CLAUDE.md depth** | ~12 KB with real project preamble (origin, stack, conventions, reuse section learned from corrections) | ~3.3 KB pure gabe-init boilerplate; project narrative lives in a stale README | **UNIFY toward gustify's shape** — gastify CLAUDE.md needs the project preamble + the UI-reuse hard rule; fix README (says React 18/ladle/P5; reality is React 19/Storybook/Phase 30+) | gustify's post-2026-06-30 CLAUDE.md rule visibly reduced P4 recurrence; gastify never got the equivalent. README drift is a live misdirection for any fresh session |
| T7 | **Mobile maturity** | placeholder (`apps/mobile/SCAFFOLD-TODO.md`, deferred) | full Expo/RN app + Maestro physical-device E2E lane (Samsung S23/WSL) | **KEEP** (product reality) — but harvest gastify's `MOBILE.md` runbook as the template when gustify's mobile starts; refresh its stale "Resume Next Session" header (dated 2026-05-15) | Divergence is intentional sequencing, not drift |
| T8 | **Legacy posture** | "**Never import legacy**" — enforced by `check-no-legacy-imports.sh` in pre-commit + CI | "**Read legacy first**" (BoletApp is the behavior reference; 8 incidents from skipping it) — not encoded anywhere | **KEEP opposite rules; encode each in its own manifest** (`legacy_reference: null` vs `{path, rule: read-before-building}`) | A genuinely opposite, correct-per-project pair — the clearest proof the knowledge is manifest-altitude, not suite-altitude |
| T9 | **Storybook/dead surfaces** | single web app; 20 active spike stories currently violating R4's "archive when settled" invariant | `frontend/` (938 files, dead since 2026-05-31, still served by root playwright config) + legacy HTML mockup trees | **Flag both for cleanup, operator-priced**: gustify = run the R4 archive sweep; gastify = decide `frontend/`'s fate (archive branch / delete) — it's 3× larger than `web/` and confuses reuse searches | Dead surfaces are reconstruction bait: reuse searches (SOP L0/L1) hit `frontend/` components that must not be adopted |
| T10 | **Ports & parallel dev** | Storybook 6006; vite 5173 | Storybook 6007 (dead)/6008; vite 5173 (**collides with gustify — documented in gastify's own playwright config comment**) | **UNIFY the port table into the manifests** (and give gastify vite a default non-5173 port) | 6 parallel-session incidents (P10) + an in-repo comment acknowledging the collision |
| T11 | **`/gabe-push` env model** | staging → promote → production (pattern 2) | single production env | **KEEP** — PUSH.md already handles this correctly per project | Working as designed; cited as the pattern the rest of the suite should copy |
| T12 | **Cross-app coupling** | R11: runtime separation rule (no shared DB/backends; API-contract integration only) | ships a "Vinculado con Gustify" UI affordance; consumes the catalog-exchange concept | **KEEP + mirror the rule** — gastify has no R11 counterpart; add it when T3 creates RULES.md | The separation rule currently lives only on one side of the boundary it protects |

## Summary counts

- **UNIFY recommended:** T3, T4, T5, T6, T10 (+T12 rule-mirror) — all are *manifest/rules layer* items, not code moves.
- **KEEP recommended:** T1, T2, T7, T8, T11 — structural/product realities the manifest absorbs.
- **CLEANUP flags:** T9 (both), T1's dead gitignore lines, T6's README, T7's stale MOBILE.md header.

The pattern across all twelve: **nothing here argues for making the twins identical** — it argues for making their *differences declared* (manifest) instead of *discovered by failure* (re-instruction). That is Constraint E ("same shape, different details") resolved in the §3 heuristic's terms.
