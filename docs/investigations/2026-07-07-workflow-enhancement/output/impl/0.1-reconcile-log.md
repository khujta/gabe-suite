# Implementation log — Step 0.1: Reconcile the three suites

- **Executed:** 2026-07-09, on the operator's "go". Branch: `chore/reconcile-hardening-pass`, commit `650d0eb` (NOT pushed, NOT merged — operator's gates).
- **E2 evidence:** every claim below carries the command/result from this run.

## What was done

1. **Safety first.** Session backup of the installed state: `scratchpad/backup-claude-gabe-pre-0.1.tar.gz` (623 KB) — in addition to the pre-existing `~/.claude/backups/gabe-suite-pre-hardening-20260702.tar.gz` noted in the hardening ledger.
2. **Read the ledger** (`~/.claude/gabe-hardening/APPLY-STATUS.md`): all 47 proposals ✅ DONE in `~/.claude`; the 4 thin shims (`commands/gabe-{debt,health,help,review}.md`) intentionally carry NO preamble ("Do not re-add") — reconciliation honored this by construction (they were byte-identical, so the copy was a no-op on them).
3. **Captured install → repo** (rsync, no deletes): 17 commands + 17 skills (incl. `gabe-myopic/method.md`) + `skills/gabe-review/references/{codex-bridge,merge-mode,post-review}.md` + `templates/hooks.json`. `git status` matched the drift-report predictions exactly (40 staged paths; +1,072/−256).
4. **Directional check that paid off:** `templates/CLAUDE.md` drift runs repo→install (repo has the corrected "Khujta Deep Behavioural Protocol"; install had the stale wrong name). NOT captured; fixed by reinstall instead.
5. **Wrote `scripts/suite-doctor.sh`** — 3-way md5 comparison (repo vs `~/.claude` vs `~/.agents`) over skills/commands/templates; exit 1 on drift. Pre-install run: exit 1, 66 drift lines (evidence: `0.1-doctor-pre-install.txt`) — claude missing gabe-docsite + stale template; agents wholesale pre-hardening.
6. **Committed** `650d0eb` with scoped paths only. Explicitly excluded: the operator's docsite WIP (`docs/site/assets/site.css`, `skills/gabe-docsite/{assets,generator}` modifications) and the untracked `docs/investigations/` folder.
7. **Reinstalled both homes:** `./install.sh` → "Installed 41/41 components."
8. **Verified:**
   - `./scripts/suite-doctor.sh` → **CLEAN**, exit 0 (evidence: `0.1-doctor-post-install.txt`).
   - `gabe-docsite` now present in both homes (`ls` both SKILL.md paths → exist).
   - 3-way md5 spot checks: `commands/gabe-next.md`, `skills/gabe-review/SKILL.md`, `references/merge-mode.md`, `templates/CLAUDE.md` — all identical across repo/claude/agents.
   - KDBP name: `grep -c "Deep Behavioural"` → 1 in both installed templates.

## Net effect

Three divergent suites → **one source of truth**. `~/.agents` (Codex) received the hardening for the first time — Claude Code and Codex now run the same suite. The F3 time-bomb (install.sh destroying the hardening) is defused: the hardening lives in git.

## Open items for the operator

- **Merge + push**: branch `chore/reconcile-hardening-pass` awaits review → merge to main → push to both remotes (via /gabe-push, as usual).
- The docsite WIP files and `docs/investigations/` remain uncommitted, untouched — commit separately when ready.

**Next step in sequence: B2 (skills-only migration).**

## Post-commit review (gabe-review-style, 2026-07-09)

Scoped review of `650d0eb` — capture fidelity, new code, hygiene:

| # | Check | Result | Severity · action |
|---|-------|--------|-------------------|
| R0 | Capture fidelity (repo == install byte-wise) | suite-doctor CLEAN pre-review — strongest possible check for a verbatim capture | — |
| R1 | Secrets/tokens/keys in added lines | 0 (all grep hits were prose about token accounting / design tokens) | — |
| R2 | Personal absolute paths added | 0 | — |
| R3 | Project-name leaks in added lines (ledger anonymization rule) | 0 — the hardening's own rule held | — |
| R4 | Frontmatter integrity, all committed md | All SKILL.md/command files valid. 4 aux files (method.md, references/*) have no frontmatter — matches existing aux convention (examples.md bare, SUITS.md with). False positive of an over-broad check | — |
| R5 | hooks.json hygiene | all 7 commands via `$ECC_ROOT` env var; no hardcoded paths/secrets. LOW note: gabe-init hooks depend on ECC tooling being present; the hardened gabe-init has a Missing-Anchor STOP for this | LOW · noted |
| R6 | **suite-doctor.sh code review** | **MEDIUM (found + fixed):** the drift check caught never-committed files under skills/ but not commands/ or templates/ — and in-place patching of commands was the original disease. Both directions now checked | **fixed in `b225d56`**; doctor re-run CLEAN |

**Verdict: APPROVE** — `650d0eb` + `b225d56` on `chore/reconcile-hardening-pass`, awaiting operator merge + push.
