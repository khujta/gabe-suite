# Command-center kickstart — paste this into a FRESH session in gabe_lens

> Generated 2026-07-10 at the gustify-window close. Launch in `/home/khujta/projects/gabe_lens`
> (add gustify read-only context if needed: `claude --add-dir /home/khujta/projects/apps/gustify`).

---

Research the **Testing Command Center** for the Gabe Suite. Mode: **ANALYZE + OPTIONS ONLY** — the
operator decides from your options report before anything is built; frontend look-and-feel
feedback comes at decision time.

READ FIRST (in order):
1. Auto-memory loads the suite state (`fable-workflow-investigation.md` — the suite is COMPLETE
   and installed; gustify's freeze window is DONE; gastify's window is pending).
2. `docs/investigations/2026-07-10-testing-command-center/BRIEF.md` — **the binding brief**:
   problem statement, the four locked operator constraints (everything-deep-linked · all three
   board families equally · static-regenerated · local-now-deployable-later), threads T1–T4,
   deliverables, anti-goals, and the two-session comms convention.
3. `skills/gabe-docs/references/evidence-doctrine.md` — the evidence layer the center renders.
4. Skim the machine-readable sources listed in the brief's "What already exists" (gustify:
   PLAN.json, thin LEDGER, proof manifests, run-status.json, journey-groups.json — read-only).

TASK: execute threads T1–T4 (external OSS scan → board families → aggregation schema →
disclosure-pattern catalog) and produce `output/01-options-report.md` (+ HTML if useful) and
`output/02-spike-plan.md` per the brief. Every option priced against the operator's constraints
and usage (solo operator, human-context-window-limited, file-based KDBP source of truth,
Jira/Trello experience). ONE recommendation, with receipts. Use web/GitHub research for T1/T2 —
prefer existing OSS over building; verify claims (stars/activity/self-hosting reality), don't
trust marketing pages.

HARD RULES: gustify app code is READ-ONLY (tests + `docs/site/` are the only writable surfaces
there, and NOT in this research phase — this phase writes only into this investigation folder).
gastify untouched. No suite skill/code changes in this phase.

RUNBOOK: model routing (operator standard) — Fable (claude-fable-5) for complex/planning/
analysis/synthesis/judging; Sonnet 5 (claude-sonnet-5) for ALL other subagent work (mechanical
scanning, per-tool evaluation, doc reading). Adversarial pass on the top recommendation before writing it (the
house pattern: a skeptic argues the opposite). Evidence style E2 (commands/URLs + what they
showed). Update `COMMS.md` in this folder when there is anything the gustify session should know.

CONTEXT NOTES:
- Trigger: Anthropic conference (2026-07-09): "coding is not the bottleneck anymore — testing
  is." The operator has MORE talk-derived points coming — leave the report structured so new
  requirements can slot in.
- The command center is likely the missing human-altitude layer over the Evidence Doctrine
  (machine proof exists; the reading surface doesn't) + the 0.5b per-project doc hub, finally built.
- One site per project is the ideal end state; gustify is the guinea pig; the gustify docs/site
  (Cifra shell) is available as a host or sibling.
